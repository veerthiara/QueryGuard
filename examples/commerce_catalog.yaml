catalog:
  name: commerce
  version: "1.0"
  dialect: postgresql

global_rules:
  - Only SELECT statements are permitted.
  - User-scoped tables must be filtered by the application user parameter.

tables:
  - name: customers
    description: Customer accounts
    aliases: [customer_accounts]
    user_scoped: false
    columns:
      - name: id
        description: Customer identifier
        type: uuid
        primary_key: true
        nullable: false
      - name: account_id
        description: Owning account identifier
        type: uuid
        nullable: false
      - name: name
        description: Customer display name
        type: varchar(255)
        nullable: false
  - name: orders
    description: Customer orders
    aliases: [sales_orders]
    user_scoped: true
    scope_strategy: direct
    scope_description: Restricted to the application account.
    business_rules:
      - Order totals are stored in cents.
    columns:
      - name: id
        description: Order identifier
        type: uuid
        primary_key: true
        nullable: false
      - name: account_id
        description: Owning account identifier
        type: uuid
        user_scope: true
        nullable: false
      - name: customer_id
        description: Customer reference
        type: uuid
        foreign_key: true
        foreign_key_target: customers.id
        nullable: false
      - name: total_cents
        description: Order total in cents
        type: integer
        nullable: false
  - name: order_items
    description: Order line items
    user_scoped: true
    columns:
      - name: id
        description: Line item identifier
        type: uuid
        primary_key: true
        nullable: false
      - name: account_id
        description: Owning account identifier
        type: uuid
        user_scope: true
        nullable: false
      - name: order_id
        description: Order reference
        type: uuid
        foreign_key: true
        foreign_key_target: orders.id
        nullable: false
      - name: product_name
        description: Purchased product name
        type: varchar(255)
        nullable: false
relationships:
  - left_table: orders
    left_column: customer_id
    right_table: customers
    right_column: id
    relationship_type: many_to_one
    description: Orders belong to customers.
  - left_table: order_items
    left_column: order_id
    right_table: orders
    right_column: id
    relationship_type: many_to_one
    description: Line items belong to orders.
